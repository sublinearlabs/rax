//! RISC-V run command

use crate::cli::common::{print_header, print_success, CliResult};

/// Execute the run command
pub fn execute_run(binary: &str, trace: bool, format: &str, output: Option<&str>) -> CliResult<()> {
    todo!()
}
