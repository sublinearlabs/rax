//! Ethereum fetch command

use riscv::cli::common::CliResult;

/// Execute the fetch command
pub fn execute_fetch(
    _block: &str,
    _rpc_url: &str,
    _format: &str,
    _output: Option<&str>,
) -> CliResult<()> {
    todo!()
}
